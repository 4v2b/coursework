<ContentDialog
    x:Class="PromotionAggeregator.Presentation.Views.GrantUserDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PromotionAggeregator.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Додати адміністратора"
     Background="{Binding Source={StaticResource NeutralColor}}"
>

    <Grid>
        <StackPanel>
            <TextBlock Text="Ел. пошта користувача" Padding="5 10 0 5"></TextBlock>
            <ComboBox x:Name="userBox" DisplayMemberPath="Email" SelectedValuePath="Email"/>

            <TextBlock Visibility="Collapsed" x:Name="errorMessage" Padding="5 10 0 5" Foreground="Crimson"/>
            <StackPanel Margin="0 10 0 5" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Margin="5" Style="{Binding Source={StaticResource CancelButton}}" Click="CancelClick" Content="Відмінити" HorizontalAlignment="Left"/>
                <Button Margin="5"  Style="{Binding Source={StaticResource AuthButton}}" Click="ConfirmClick" Content="Підтвердити" HorizontalAlignment="Right"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</ContentDialog>
